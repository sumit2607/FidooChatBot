package com.example.fidoochatbot.Repo

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import com.example.fidoochatbot.Network.ChatbotApi

class MovieRepository(private val chatbotApi: ChatbotApi) {

    //private val movieLiveData: MutableLiveData<listofdata> = MutableLiveData()

    //fun getData(): LiveData<listofdata> {
      //  return movieLiveData
    }


    suspend fun getMovie() {
    //    val result = movieApi.getMovies()
      //  if (result.body() != null) {
        //    movieLiveData.postValue(result.body())
        }

